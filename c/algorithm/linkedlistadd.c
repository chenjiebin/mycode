// 链表数字相加
// 小象课程第一章链表练习
// 例如输入2->4->3、5->6->4，相加得到7->0->8
#include <stdio.h>
#include <math.h>

struct Node {
	int num
	struct Node *next
}

struct Node *Add(Node pHead1, Node pHead2) 
{
	struct Node *pSum;
	struct Node *pTail = pSum;
	struct Node *p1 = pHead1->next;
	struct Node *p2 = pHead2->next;
	struct pCur;
}

int main()
{

}