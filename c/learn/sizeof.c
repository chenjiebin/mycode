#include <stdio.h>

int main()
{
    short int i;
    int j;
    long int k;

    printf("sizeof(i) = %d", sizeof(i));
    printf("sizeof(j) = %d", sizeof(j));
    printf("sizeof(k) = %d", sizeof(k));
}
